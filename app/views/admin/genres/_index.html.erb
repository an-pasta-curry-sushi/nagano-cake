<% genres.each do |genre| %>
  <tr>
    <td class="align-middle" style="width 20%"><h5 class="ml-2"><%= genre.name %></h5></td>
    <td class="text-right">
      <%= link_to "編集する", edit_admin_genre_path(genre), class: "btn btn-success" %>
    </td>
  </tr>
<% end %>
